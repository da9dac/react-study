import { Box, Typography } from "@mui/material";

export default function Content() {
    return (
        <Box
            component="main"
            sx={{
                width: '100%',
                flexGrow: 1,
                padding: 2,
                marginTop: '9vh'
            }}
            >
        </Box>
    );
}